[
    {
        "text": "Consider the following code segment:<div class='border p-3 my-3'><code>double val = 15.85;<br>int cost = (int) val;<br>cost++;<br>System.out.println(cost);</code></div><p class='mt-3'>What is printed as a result of executing the code segment?</p>",
        "correctAnswers": [
            "16"
        ],
        "answerDetail": "The <code>double</code> <code>val</code> is first cast to an <code>int</code>, which truncates the decimal, resulting in <code>15</code>. The <code>cost++</code> statement then increments the value to <code>16</code>."
    },
    {
        "text": "What is the correct syntax for declaring a constant <code>MAX_SCORE</code> with a value of 100 that cannot be changed?",
        "correctAnswers": [
            "final int MAX_SCORE = 100;",
            "final int MAX_SCORE = 100",
            "final int MAX_SCORE=100;",
            "final int MAX_SCORE=100"
        ],
        "answerDetail": "The <code>final</code> keyword ensures that once the value is assigned, it cannot be modified, which is the definition of a constant."
    },
    {
        "text": "What is the result of the expression <code>14 / 5 * 2.0</code>?",
        "correctAnswers": [
            "4.0"
        ],
        "answerDetail": "First, integer division <code>14 / 5</code> is performed, which results in <code>2</code> (the decimal is truncated). Then, <code>2</code> is multiplied by <code>2.0</code>, resulting in <code>4.0</code>."
    },
    {
        "text": "Consider the following code, which is intended to calculate the average of three scores. What value is stored in <code>average</code>?<div class='border p-3 my-3'><code>int score1 = 80;<br>int score2 = 90;<br>int score3 = 95;<br>double average = (score1 + score2 + score3) / 3;</code></div>",
        "correctAnswers": [
            "88.0"
        ],
        "answerDetail": "The sum of the scores (265) is divided by the integer 3, which results in the integer 88 due to truncation. This is then assigned to the double variable <code>average</code>."
    },
    {
        "text": "What is the output of the following code segment?<div class='border p-3 my-3'><code>int count = 5;<br>count *= 2;<br>count--;<br>System.out.println(\"Result: \" + count + 1);</code></div>",
        "correctAnswers": [
            "Result: 91",
            "\"Result: 91\""
        ],
        "answerDetail": "<code>count</code> becomes 10, then 9. The expression then concatenates \"Result: \" with 9 to get \"Result: 9\", and then concatenates that with 1."
    },
    {
        "text": "What is the value of <code>Math.pow(2, 3) + (int) 5.9</code>?",
        "correctAnswers": [
            "13.0"
        ],
        "answerDetail": "<code>Math.pow(2, 3)</code> evaluates to 8.0. The expression <code>(int) 5.9</code> evaluates to 5. The sum of 8.0 and 5 is 13.0."
    },
    {
        "text": "A programmer wants to store a user's GPA, which can have a decimal value (e.g., 3.85). Which of the following is the most appropriate <code>Scanner</code> method to read this value?",
        "correctAnswers": [
            "input.nextDouble()",
            "Scanner.nextDouble()",
            "nextDouble()",
            "input.nextdouble()",
            "scanner.nextdouble()",
            "nextdouble()"
        ],
        "answerDetail": "This method is specifically designed to read a value of type <code>double</code>, which is appropriate for storing a GPA."
    },
    {
        "text": "Consider the following code segment:<div class='border p-3 my-3'><code>int a = 10;<br>int b = 4;<br>double result = a / b;<br>System.out.println(result);</code></div><p class='mt-3'>What is printed as a result of executing the code segment?</p>",
        "correctAnswers": [
            "2.0"
        ],
        "answerDetail": "The integer division <code>10 / 4</code> results in <code>2</code>. This integer value is then promoted to a <code>double</code> when it is assigned to the <code>result</code> variable."
    },
    {
        "text": "What is the result of <code>Math.abs(-15.5) + Math.round(3.4)</code>?",
        "correctAnswers": [
            "18.5"
        ],
        "answerDetail": "<code>Math.abs(-15.5)</code> is 15.5. <code>Math.round(3.4)</code> rounds to the nearest whole number, which is 3. The sum is 15.5 + 3 = 18.5."
    },
    {
        "text": "Consider the code segment below:<div class='border p-3 my-3'><code>int x = 3;<br>int y = 5;<br>y = x++;</code></div><p class='mt-3'>After this code executes, what is the final value of <code>y</code>?</p>",
        "correctAnswers": [
            "3"
        ],
        "answerDetail": "Because <code>x++</code> is a postfix operation, the original value of <code>x</code> (3) is assigned to <code>y</code> first."
    }
]