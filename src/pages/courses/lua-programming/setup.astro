---
import Divider from "../../../components/ui/Divider.astro";
import Layout from "../../../layouts/Layout.astro";
---

<!--
  PLACEHOLDER — this page is yours to write.

  It only exists so the course index has no broken link. Replace everything
  between the <h1> and the closing </section> with the real setup guide.

  What the activities downstream assume you will have covered:
    - A Lua interpreter on PATH, invoked as `lua main.lua` from a terminal.
      Activity 1.4 is authored to be true for both Lua 5.4 and LuaJIT, but it
      calls out where their output differs, so it helps if this page names
      which one students installed.
    - VS Code, with a project folder students can create .lua files in.
    - A terminal inside VS Code, and how to open it.
    - Unit 2 additionally needs LÖVE installed; that is covered on 2-1, not here.
-->

<Layout>
  <div class="container my-4">
    <div class="row justify-content-center">
      <div class="col-sm-12 col-md-10">
        <a
          href="/courses/lua-programming/"
          class="btn btn-sm btn-outline-light mb-3"
        >
          <i class="si-arrow-left"></i> Back
        </a>

        <h1>Setup: Installing Lua and VS Code</h1>
        <Divider variant="8" classes="mb-5" />

        <section id="setup">
          <h2 class="text-center my-5 slow-flash">Coming when it's ready</h2>

          <p class="text-center">
            In the meantime, the activities assume you can create a file named{" "}
            <b class="text-sn-theme">main.lua</b> in a folder and run it from a
            terminal.
          </p>
        </section>

        <div class="d-flex justify-content-between mt-5">
          <a class="btn btn-outline-light" href="/courses/lua-programming/">
            Course Index
          </a>
          <a
            class="btn btn-outline-primary"
            href="/courses/lua-programming/unit-1/1-1"
          >
            Next: Meet Lua <i class="si-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>
  </div>
</Layout>
